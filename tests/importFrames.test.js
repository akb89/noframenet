/**
 * Behavior tests for importFrames script
 */

import chai from 'chai';
import mochAsync from './async.test';

const should = chai.should();

describe('importFrames', () => {
  before(mochAsync(async () => {

  }));
  it('', () => {

  });
});
